---
year: 2024
question_id: "2024-44"
subject: 计算机组成原理
chapter: 存储系统
knowledge_point: 存储管理
type: 综合题
difficulty: 4
frequency: 高频
exam_count: 11
tags: ["计组", "页式存储", "指令"]
---

# 2024年第44题

**考点**：存储管理 | 指令编码 | 页式存储

**题干**：
对于题43中的计算机M，C语言程序P包含的语句"sum+=a[i];"在M中对应的指令序列S如下。

```
slli r4,r2,2    // R[r4]←R[r2] << 2
add r4,r3,r4    // R[r4]←R[r3] + R[r4]
lw r5,0(r4)     // R[r5]←M[R[r4] + 0]
add r1,r1,r5    // R[r1]←R[r1] + R[r5]
```

已知变量i、sum和数组a都为int型，通用寄存器r1~r5的编号为01H~05H。请回答下列问题。

1) 根据指令序列S中每条指令的功能, 写出存放数组a的首地址、变量i和sum的通用寄存器编号。(3分)

2) 已知M为小端方式计算机, 采用页式存储管理方式, 页大小为4KB。若执行到指令序列S中第1条指令时, i=5且r1和r3的内容分别为0000 1332H和0013 DFF0H, 从地址0013 DFF0H开始的存储单元内容如下图所示, 则执行 "sum+=a[i];" 语句后, a[i]的地址、a[i]和sum的机器数分别是什么 (用十六进制表示)? a[i]所在页的页号是多少? 此次执行中, 数组a至少存放在几页中? (5分)

![image](https://cdn-mineru.openxlab.org.cn/result/2026-06-09/61a7441a-e21c-435b-9476-47f9ff01a858/5bce1f327c3e916872c6bb75820fe7267287776203a5a2618ca94e372ff0224c.jpg)

3）指令"slli r4, r2, 2"的机器码是什么（用十六进制表示）？若数组a改为short类型，则指令序列S中slli指令的汇编形式应是什么？（2分）

**答案**：
## 44. 【王道书 4.3 节】

## 1）【标准答案】

a 的首地址存放在 r3 中；i 存放在 r2 中；sum 存放在 r1 中。

【分析】

r2 中存放的是变量 i 的值，R[r4]←R[r2]<<2 的功能是将 i 左移两位，即将 i×4 的值送到 r4。r3 中存放的是数组 a 的首地址，即 a[0] 的地址，R[r4]←R[r3]+R[r4] 的功能是计算 addr(a[0]) + i×4，即将 a[i] 的地址送到 r4。R[r5]←M[R[r4]+0] 的功能就是将 a[i] 送到 r5。r1 中存放的是变量 sum 的值，R[r1]←R[r1]+R[r5] 的功能是将 sum + a[i] 的结果送回 r1。

## 2）【标准答案】

a[i]的地址为0013 E004H；a[i]的机器数为FFFF ECDCH；sum的机器数为0000 000EH；a[i]所在页的页号是0013EH；数组a至少存放在2页中。

【分析】

数组 a 的首地址为 0013 DFF0H，从表中可以看出，a[5]的地址为 0013 E004H，页大小为 4KB，因此页内偏移量为低 12 位，页号为地址高 20 位即 0013EH，M 为小端方式计算机，根据表格可知，a[5]的机器数为 FFFF ECDCH。sum 的初值为 0000 1332H，执行 sum+=a[i]后，sum 的值更新为 0000 1332H + FFFF ECDCH = 0000 000EH。表格中的数据包含了页号为 0013DH 和 0013EH 两个不同页面的数据，故数组 a 至少存放在 2 页中。

## 3）【标准答案】

指令机器码为 0021 2213H。汇编形式是 slli r4, r2, 1。

【分析】

机器码 = 0000000 00010 00010 010 00100 0010011 = 0021 2213H。若数组 a 改为 short 型，则每个元素占 2 字节，a[i] 的地址为 addr(a[0]) + i×2，因此 slli 指令的汇编形式是 slli r4, r2, 1。
---

## 解题思路

### 第一步：追踪指令序列确定寄存器用途
slli r4,r2,2将r2左移2位(乘4)，r2是下标i。add r4,r3,r4用r3作基址，r3是数组首址。lw加载元素到r5，add r1,r1,r5累加到r1，r1是sum。寄存器分配：r1=sum, r2=i, r3=数组a首址。

### 第二步：计算数组元素地址和机器数
i=5，首址0013DFF0H，a[i]地址=0013DFF0H+5×4=0013E004H。小端方式从低地址读4字节：DC EC FF FF→FFFFECDCH(补码负数)。sum初值1332H，加FFFFECDCH得000EH。

### 第三步：确定页号和跨页情况
页大小4KB=2^12B，页内偏移12位，页号高20位。a[i]地址0013E004H，页号0013EH。首址0013DFF0H在页0013DH，a[5]在页0013EH，跨越2页边界。数组至少存2页。

### 第四步：计算指令机器码和寻址适配
slli格式：功能码7位+立即数5位(shamt=2)+源寄存器5位(r2=00010)+funct3=010+目标寄存器5位(r4=00100)+opcode=0010011。拼接为0021 2213H。若short型元素占2字节，左移量改为1。

## 易错点分析

### 易错点1：小端方式字节序理解错误
❌ 错误读取：地址0013E004H存储DC EC FF FF，按大端理解为DCECFFFFH，计算sum错误。
✅ 正确理解：小端方式低字节存低地址。4字节从低到高为DC、EC、FF、FF，组合为FFFFECDCH(高字节FFH在数值高位)。读取时逆序组合，内存顺序≠数值顺序。

### 易错点2：跨页判断只看地址差值
❌ 错误判断：首址0013DFF0H到a[5]的0013E004H差值20字节<4KB，认为在同一页内。
✅ 正确理解：页边界按页号划分，不是按距离。0013DFF0H页号0013DH(高20位)，0013E004H页号0013EH，页号不同即跨页。地址0013DFFFH是页0013DH末尾，0013E000H是页0013EH起始，数组横跨边界。

### 易错点3：指令机器码编码字段顺序
| RISC-V I型指令格式 | slli r4,r2,2机器码 | 各字段含义 |
|-------|-------|------|
| imm[11:0]12位 | 0000000 00010(shamt=2) | 立即数/移位量 |
| rs1[5位] | 00010(r2) | 源寄存器编号 |
| funct3[3位] | 010(左移逻辑) | 功能码细分 |
| rd[5位] | 00100(r4) | 目标寄存器编号 |
| opcode[7位] | 0010011(立即数运算) | 操作码 |