---
year: 2016
question_id: "2016-44"
subject: 计算机组成原理
chapter: IO系统
knowledge_point: 中断IO
type: 综合题
difficulty: 4
frequency: 高频
exam_count: 13
tags: ["计组", "中断", "IO"]
---

# 2016年第44题

**考点**：中断IO | 异步串行通信 | 中断响应 | CPI计算

**题干**：
假定 CPU 主频为 50MHz，CPI 为 4。设备 D 采用异步串行通信方式向主机传送 7 位 ASCII 字符，通信规程中有 1 位奇校验位和 1 位停止位，从 D 接收启动命令到字符送入 I/O 端口需要 0.5ms。请回答下列问题，要求说明理由。

1) 每传送一个字符，在异步串行通信线上共需传输多少位？在设备 D 持续工作过程中，每秒最多可向 I/O 端口送入多少个字符？

2) 设备 D 采用中断方式进行输入/输出，示意图如下。

![image](https://cdn-mineru.openxlab.org.cn/result/2026-06-09/61a7441a-e21c-435b-9476-47f9ff01a858/f17eb251fd68a902ce5deb232dc19adccc733d4e10afa7b0c3d190346ab0a490.jpg)

I/O 端口每收到一个字符申请一次中断，中断响应需 10 个时钟周期，中断服务程序共有 20 条指令，其中第 15 条指令启动 D 工作。若 CPU 需从 D 读取 1000 个字符，则完成这一任务所需时间大约是多少个时钟周期？CPU 用于完成这一任务的时间大约是多少个时钟周期？在中断响应阶段 CPU 进行了哪些操作？

**答案**：
## 44.【王道书7.3节】

## 1)【标准答案】

每传送一个 ASCII 字符，需要传输的位数有 1 位起始位、7 位数据位（ASCII 字符占 7 位）、1 位奇校验位和 1 位停止位，故总位数为 $1 + 7 + 1 + 1 = 10$ 。（2 分）

I/O 端口每秒钟最多可接收 1000/0.5 = 2000 个字符。(1 分)

【评分说明】对于第一问，若考生回答总位数为9，则给1分。

## 2)【标准答案】

一个字符传送时间包括: 设备 D 将字符送 I/O 端口的时间、中断响应时间和中断服务程序前 15 条指令的执行时间。时钟周期为 $1 \div 50 \mathrm{MHz} = 20 \mathrm{ns}$ , 设备 D 将字符送 I/O 端口的时间为 $0.5 \mathrm{ms} / 20 \mathrm{ns} = 2.5 \times 10^{4}$ 个时钟周期。一个字符的传送时间大约为 $2.5 \times 10^{4} + 10 + 15 \times 4 = 25070$ 个时钟周期。完成 1000 个字符传送所需时间大约为 $1000 \times 25070 = 25070000$ 个时钟周期。(3 分) CPU 用于该任务的时间大约为 $1000 \times (10 + 20 \times 4) = 9 \times 10^{4}$ 个时钟周期。(1 分) 在中断响应阶段, CPU 主要进行以下操作: 关中断、保护断点和程序状态、识别中断源。(2 分)

## 【评分说明】

① 对于第一问，若答案是 25070020，则同样给分；若答案是 25000000 或 25000020，则给 2 分。如果没有给出分布计算步骤，但算式和结果正确，同样给分。

② 对于第三问，只要回答关中断和保护断点，就给2分，其他答案酌情给分。
---

## 解题思路

**问题1：串行通信位数与传输速率**
传输位数：1起始+7数据+1校验+1停止=10位。准备数据0.5ms，每秒最多1000÷0.5=2000个字符。

**问题2：中断方式时间计算**
一个字符传送时间=设备准备时间+中断响应+中断服务前15条指令=0.5ms/(20ns)个周期+10+15×4=25070周期。1000个字符=25070000周期。CPU处理时间=1000×(10+20×4)=90000周期。中断响应阶段：关中断、保护断点和程序状态、识别中断源。

## 易错点分析

### 易错点1：传输总位数按数据位计算
❌ 错误理解：ASCII字符7位，加1位校验和1位停止共9位传输。
✅ 正确理解：异步串行通信格式：1起始位+n数据位+1校验位+1停止位。起始位用于同步，不能省略。本题传输10位（1+7+1+1），不是9位。

### 易错点2：CPU总时间包含设备准备时间
❌ 错误理解：CPU完成任务总时间=设备准备时间+中断处理时间，1000字符共25070000周期。
✅ 正确理解：设备准备数据时CPU可执行其他任务，不计入"CPU用于该任务的时间"。CPU实际处理仅中断响应(10周期)和中断服务(20×4周期)，共90000周期。设备准备时间是任务完成总时间，非CPU专用时间。