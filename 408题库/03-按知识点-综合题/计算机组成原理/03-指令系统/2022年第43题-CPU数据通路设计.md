---
year: 2022
question_id: "2022-43"
subject: 计算机组成原理
chapter: 指令系统
knowledge_point: 数据通路
type: 综合题
difficulty: 5
frequency: 高频
exam_count: 13
tags: ["计组", "数据通路", "ALU"]
---

# 2022年第43题

**考点**：数据通路 | ALU | 标志位 | 取指周期

**题干**：
某CPU中部分数据通路如题43图所示，其中，GPRs为通用寄存器组；FR为标志寄存器，用于存放ALU产生的标志信息；带箭头虚线表示控制信号，如控制信号Read、Write分别表示主存读、主存写，MDRin表示内部总线上数据写入MDR，MDRout表示MDR的内容送内部总线。

![image](https://cdn-mineru.openxlab.org.cn/result/2026-06-09/61a7441a-e21c-435b-9476-47f9ff01a858/0fa0debf0f2d361035d1f8872fb9cf08ba5be473982877f76ad72667ecc70d1b.jpg)

请回答下列问题。

1) 设 ALU 的输入端 A、B 及输出端 F 的最高位分别为 $A_{15}$、$B_{15}$ 及 $F_{15}$，FR 中的符号标志和溢出标志分别为 SF 和 OF，则 SF 的逻辑表达式是什么？A 加 B、A 减 B 时 OF 的逻辑表达式分别是什么？要求逻辑表达式的输入变量为 $A_{15}$、$B_{15}$ 及 $F_{15}$。

2）为什么要设置暂存器Y和Z?

3）若 GPRs 的输入端 rs、rd 分别为所读、写的通用寄存器的编号，则 GPRs 中最多有多少个通用寄存器？rs 和 rd 来自图中的哪个寄存器？已知 GPRs 内部有一个地址译码器和一个多路选择器，rd 应连接地址译码器还是多路选择器？

4）取指令阶段（不考虑 PC 增量操作）的控制信号序列是什么？若从发出主存读命令到主存读出数据并传送到 MDR 共需 5 个时钟周期，则取指令阶段至少需要几个时钟周期？

5）图中控制信号由什么部件产生？图中哪些寄存器的输出信号会连到该部件的输入端？

**答案**：
## 43.【王道书5.3节】

## 1)【标准答案】

$\mathrm{SF} = \mathrm{F}_{15}$ ；加运算时， $\mathrm{OF} = \overline{\mathrm{A}_{15}}\cdot \overline{\mathrm{B}_{15}}\cdot \mathrm{F}_{15} + \mathrm{A}_{15}\cdot \mathrm{B}_{15}\cdot \overline{\mathrm{F}_{15}}$ ；减运算时， $\mathrm{OF} = \overline{\mathrm{A}_{15}}\cdot \mathrm{B}_{15}\cdot \mathrm{F}_{15}+$ 

$$
\mathrm{A} _ {1 5} \cdot \overline {{\mathrm{B} _ {1 5}}} \cdot \overline {{\mathrm{F} _ {1 5}}} 。
$$

【分析】

符号标志 SF 表示运算结果的正负性，因此 $SF = F_{15}$ 。

对于加法运算 $\mathrm{A} + \mathrm{B} \rightarrow \mathrm{F}$ , 若 $\mathrm{A} 、 \mathrm{B}$ 为负, 且 $\mathrm{F}$ 为正, 说明发生溢出; 或者, 若 $\mathrm{A} 、 \mathrm{B}$ 为正, 且 $\mathrm{F}$ 为负, 也说明发生溢出。因此, 加运算时, $\mathrm{OF} = \overline{\mathrm{A}_{15}} \cdot \overline{\mathrm{B}_{15}} \cdot \mathrm{F}_{15} + \mathrm{A}_{15} \cdot \mathrm{B}_{15} \cdot \overline{\mathrm{F}_{15}}$ 。

对于减法运算 A-B→F，若 A 为负、B 为正，且 F 为正，说明发生溢出；或者，若 A 为正、B 为负，且 F 为负，也说明发生溢出。因此，减运算时， $OF=\overline{A_{15}}\cdot B_{15}\cdot F_{15}+A_{15}\cdot\overline{B_{15}}\cdot\overline{F_{15}}$ 。

2)【标准答案】

因为单总线结构中每个时刻总线上只有一个数据有效，而 ALU 有两个输入端和一个输出端，因此需要设置 Y 和 Z 两个暂存器，以缓存 ALU 的一个输入端和输出端数据。

【分析】

在单总线数据通路中，每个时刻总线上只有一个数据有效。由于 ALU 是一个没有存储功能的组合逻辑元件，在其执行运算时必须保持两个输入端同时有效，因此先将一个操作数经内部总线送入暂存器 Y 保存，Y 的内容在 ALU 的左输入端始终有效，再将另一个操作数经内部总线直接送到 ALU 的右输入端。此外，ALU 的输出端也不能直接与总线相连，否则其输出会通过总线反馈到输入端，影响运算结果，因此将运算结果暂存在暂存器 Z 中。

3)【标准答案】

GPRs 中最多有 $2^{4} = 16$ 个通用寄存器；rs 和 rd 来自指令寄存器 IR；rd 应连接地址译码器。

【分析】

由图可知，rs 和 rd 表示寄存器编号，rs 和 rd 的位数都是 4bit。

4)【标准答案】

取指阶段的控制信号序列为：①PCout, MARin ②Read ③MDRout, IRin。取指阶段至少需要7个时钟周期。

【分析】

取指阶段需要根据 PC 的内容取出主存中的指令，并存入 IR。各个序列的解释：①将 PC 的内容写入 MAR；②读主存，并将读出的数据写入 MDR；③将 MDR 的内容写入 IR。其中，步骤①需要 1 个时钟周期，步骤②需要 5 个时钟周期，步骤③需要 1 个时钟周期。

5)【标准答案】

图中控制信号由控制部件（CU）产生。指令寄存器 IR 和标志寄存器 FR 的输出信号会连到控制部件的输入端。
---

## 解题思路

### 第一步：理解题目核心
分析题目考查的核心知识点

### 第二步：逐项分析
逐一分析各选项，用✅❌标记正确与否

### 第三步：确认答案
综合分析得出答案

## 易错点分析

### 易错点1：溢出标志逻辑表达式符号位判断错误
❌ 错误理解：加法时OF=$A_{15}·B_{15}·F_{15}+\overline{A_{15}}·\overline{B_{15}}·\overline{F_{15}}$，认为同号相加结果变号才溢出。
✅ 正确理解：加法OF=$\overline{A_{15}}·\overline{B_{15}}·F_{15}+A_{15}·B_{15}·\overline{F_{15}}$（正+正=负或负+负=正）。减法OF=$\overline{A_{15}}·B_{15}·F_{15}+A_{15}·\overline{B_{15}}·\overline{F_{15}}$（正-负=负或负-正=正）。注意减法中B符号需取反判断。

### 易错点2：取指周期时钟数计算遗漏访存等待
❌ 错误计算：取指需3步操作，共3个时钟周期（PCout+MARin 1周期，Read 1周期，MDRout+IRin 1周期）。
✅ 正确计算：步骤②Read主存操作需5个时钟周期等待数据，总共1+5+1=7周期。访存操作时间由存储器速度决定，不是1周期。

### 易错点3：暂存器Y和Z作用理解
| 暂存器Y | 暂存器Z | 单总线结构约束 |
|-------|-------|------|
| 保存ALU左操作数 | 保存ALU运算结果 | 每时刻总线仅1个数据有效 |
| ALU输入端需同时有效 | 防止输出反馈到输入 | ALU无存储功能需外部寄存器 |