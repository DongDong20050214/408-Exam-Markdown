---
year: 2024
question_id: "2024-41"
subject: 数据结构
chapter: 图
knowledge_point: 拓扑排序
type: 综合题
difficulty: 4
frequency: 高频
exam_count: 10
tags: ["数据结构", "图", "拓扑排序"]
---

# 2024年第41题

**真题出处**：[2024年第41题](../../../01-真题原卷/2024年408真题.md#41)

**考点**：拓扑排序 | 有向图 | 图的遍历

**题干**：
2023年10月26日，神舟十七号载人飞船发射取得圆满成功，再次彰显了中国航天事业的辉煌成就。载人航天工程是包含众多子工程的复杂系统工程，为了保证工程的有序开展，需要明确各子工程的前导子工程，以协调各子工程的实施。该问题可以简化、抽象为有向图的拓扑序列问题。已知有向图G采用邻接矩阵存储，类型定义如下。

```c
typedef struct    //图的类型定义
{
    int numVertices, numEdges;    //图的顶点数和有向边数
    char VerticesList[MAXV];    //顶点表，MAXV为已定义常量
    int Edge[MAXV][MAXV];    //邻接矩阵
} MGraph;
```

请设计算法：int uniquely(MGraph G)，判定G是否存在唯一的拓扑序列，若是，则返回1，否则返回0。要求如下。

1）给出算法的基本设计思想。（4分）

2）根据设计思想，采用C或C++语言描述算法，关键之处给出注释。（9分）

**答案**：
## 41. 【王道书6.4节】

## 1）【标准答案】

算法的基本设计思想：

建立图 G 各顶点的入度表 degree[]。

选择入度为 0 的顶点 v，将 v 的所有邻接点的入度减 1，重复执行这个过程。若每次选中的入度为 0 的顶点有且仅有一个，且共进行了 G.numVertices 次，则意味着存在唯一的拓扑序列，返回 1，否则不存在拓扑序列，或者存在多个拓扑序列，返回 0。

## 2）【标准答案】

用 C 语言描述的算法如下:

```javascript
int uniquely(MGraph G){//判定有向图是否存在唯一的拓扑序列
    int *degree,i,j,count=0,in0=-1,prev_in0;
    degree=(int*)malloc(G.numVertices*sizeof(int));
    for(j=0;j<G.numVertices;j++){//计算各顶点的入度
    degree[j]=0;
    for(i=0;i<G.numVertices;i++)
    degree[j]+=G.Edge[i][j];
    if(degree[j]==0){
    if(in0==-1) in0=j; //入度为0的顶点
    else in0=-2; //有多个入度为0的顶点
    }
}
while(in0>=0){
    count++;
    prev_in0=in0;
    in0=-1;
    for(j=0;j<G.numVertices;j++)
    if(G.Edge[prev_in0][j]>0){
    if(--degree[j]==0){ //邻接点入度值减1
    if(in0==-1) in0=j; //入度为0的顶点
    else in0=-2; //有多个入度为0的顶点
    }
    }
}
free(degree);
if(count==G.numVertices) return 1;
else return 0;
}
```
---

## 解题思路

**核心思想**：模拟拓扑排序，每次检查入度为0的顶点是否唯一

**解题步骤**：
1. 计算各顶点入度，标记入度为0的顶点(in0)
2. 若有多个入度为0顶点，in0=-2，拓扑序列不唯一
3. 选中唯一的入度为0顶点，邻接点入度-1，count++
4. 重复步骤2-3，直到in0<0或全部顶点处理完
5. count=n返回1(唯一)，否则返回0

## 易错点分析

### 易错点1：唯一性判定条件
❌ 错误理解：存在拓扑序列即返回1
✅ 正确理解：每次选择入度为0的顶点必须有且仅有一个，才能保证唯一性

### 易错点2：in0标记含义
| in0值 | 含义 | 处理 |
|-------|------|------|
| -1 | 无入度为0顶点 | 初始状态或所有顶点已处理 |
| -2 | 多个入度为0顶点 | 拓扑序列不唯一 |
| ≥0 | 唯一入度为0顶点下标 | 继续处理 |