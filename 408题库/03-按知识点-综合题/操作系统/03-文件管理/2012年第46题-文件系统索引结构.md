---
year: 2012
question_id: "2012-46"
subject: 操作系统
chapter: 文件管理
knowledge_point: 索引结构
type: 综合题
difficulty: 4
frequency: 中频
exam_count: 12
tags: ["操作系统", "文件系统", "索引"]
---

# 2012年第46题

**考点**：文件系统 | 索引结构 | 连续分配 | 文件长度

**题干**：
某文件系统空间的最大容量为 4TB (1T = 2⁴⁰B)，以磁盘块为基本分配单位，磁盘块大小为 1KB。文件控制块 (FCB) 包含一个 512B 的索引表区。请回答下列问题。

1) 假设索引表区仅采用直接索引结构，索引表区存放文件占用的磁盘块号。索引表项中块号最少占多少字节？可支持的单个文件最大长度是多少字节？

2) 假设索引表区采用如下结构：第 0~7 字节采用<起始块号, 块数>格式表示文件创建时预分配的连续存储空间，其中起始块号占 6B，块数占 2B；剩余 504 字节采用直接索引结构,一个索引项占 6B，则可支持的单个文件最大长度是多少字节？为了使单个文件的长度达到最大，请指出起始块号和块数分别所占字节数的合理值并说明理由。

**答案**：
46.【王道书4.1节】

## 1)【标准答案】

文件系统存储空间共有块数 $2^{42} \div 2^{10} = 2^{32}$ 。为表示 $2^{32}$ 个块号，索引表项占 $32 \div 8 = 4\mathrm{B}$ 。（2分）512字节可存放 $2^{7}$ 个索引表项，故最大文件长度为 $2^{7} \times 2^{10} = 2^{17}\mathrm{B} = 128\mathrm{KB}$ 。（2分）

【评分说明】过程描述正确，但计算结果不正确，可酌情给分。

## 2)【标准答案】

当块号占6字节、块数占2字节时，最大文件长度为 $2^{16}\times2^{10}+(504\div6)\times2^{10}=64MB+84KB=65620KB$ 。（2分）

合理的起始块号和块数所占字节数分别为 4, 4（或 1, 7 或 2, 6 或 3, 5）。（2 分）理由：块数占 4B 或以上，就可表示 4TB 大小的文件长度，达到文件系统的空间上限。（2 分）

## 【分析】

这里考查的分配方式不同于我们熟悉的三种经典分配方式，但题中给出了详细的解释。所求的单个文件最大长度共包含两部分：预分配的连续空间和直接索引区。

连续区块数占 2B，共可以表示 $2^{16}$ 个磁盘块，即 $2^{26}\mathrm{B}$ 。直接索引区共 $504\mathrm{B} \div 6\mathrm{B} = 84$ 个索引项。所以该系统可支持的单个文件最大长度是 $2^{26}\mathrm{B} + 84\mathrm{KB}$ 。

为了使单个文件的长度达到最大，应使连续区的块数字段表示的空间大小尽可能接近系统最大容量4TB。分别设起始块号和块数分别占4B，这样起始块号可以寻址的范围是 $2^{32}$ 个磁盘块，共4TB，即整个系统空间。同样，块数字段可以表示最多 $2^{32}$ 个磁盘块，共4TB。

【评分说明】过程描述正确，但计算结果不正确，可酌情给分。只要答出起始块号和块数所占字节数的任意合理值，均可得1分；理由说明正确得1分。
---

## 解题思路

索引结构计算各级容量，直接+一级+二级+三级地址项逐层扩展。访盘次数=查目录+读节点+读数据块。磁盘调度算法按寻道距离优化。


### 易错点1：间接地址块容量计算遗漏指针开销
❌ 错误：4KB块全存数据地址，容量4KB。
✅ 正确：每个地址4B，一级间接块存1024个地址，指向1024个数据块，容量1024×4KB。

### 易错点2：访盘次数计算
目录已在内存不计入；索引节点和数据块各计1次；间接寻址需读间接块。

## 易错点分析

