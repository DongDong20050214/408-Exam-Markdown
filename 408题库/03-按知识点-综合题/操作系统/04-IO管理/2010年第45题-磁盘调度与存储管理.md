---
year: 2010
question_id: "2010-45"
subject: 操作系统
chapter: IO管理
knowledge_point: 磁盘调度
type: 综合题
difficulty: 4
frequency: 高频
exam_count: 13
tags: ["操作系统", "磁盘调度", "CSCAN"]
---

# 2010年第45题

**真题出处**：[2010年第45题](../../../01-真题原卷/2010年408真题.md#45)

**考点**：磁盘调度 | CSCAN | 位图法 | 磁盘访问时间

**题干**：
假设计算机系统采用 CSCAN（循环扫描）磁盘调度策略，使用 2KB 的内存空间记录 16384 个磁盘块的空闲状态。

1）请说明在上述条件下如何进行磁盘块空闲状态的管理。

2) 设某单面磁盘旋转速度为每分钟 6000 转, 每个磁道有 100 个扇区, 相邻磁道间的平均移动时间为 $1 \mathrm{~ms}$ 。若在某时刻, 磁头位于 100 号磁道处, 并沿着磁道号增大的方向移动 (见下图), 磁道号请求队列为 50, 90, 30, 120, 对请求队列中的每个磁道需读取 1 个随机分布的扇区, 则读完这 4 个扇区点共需要多少时间? 要求给出计算过程。

3) 如果将磁盘替换为随机访问的 Flash 半导体存储器（如 U 盘、SSD 等），是否有比 CSCAN 更高效的磁盘调度策略？若有，给出磁盘调度策略的名称并说明理由；若无，说明理由。

![image](https://cdn-mineru.openxlab.org.cn/result/2026-06-09/61a7441a-e21c-435b-9476-47f9ff01a858/27fa0af6edb80473fec5767187454927133361d76f5492efc668161eb491a130.jpg)

**答案**：
45.【王道书5.3节】

1)【标准答案】

用位图表示磁盘的空闲状态。每位表示一个磁盘块的空闲状态，共需要 $16384 / 32 = 512$ 字 $= 512 \times 4$ 字节 $= 2\mathrm{KB}$ ，正好可放在系统提供的内存中。

2)【标准答案】

采用 CSCAN 调度算法，访问磁道的顺序为 120, 30, 50, 90，则移动的磁道长度为 170，总移动磁道时间为 170ms。

由于转速为 6000 转/分（rpm），因此平均旋转延迟为 5ms，总旋转延迟时间 = 20ms。

由于转速为 6000rpm，因此读取一个磁道上一个扇区的平均读取时间为 0.1ms，总读取扇区的时间为 0.4ms。

读取上述磁道上所有扇区所花的总时间为 190.4ms。

【分析】

采用 CSCAN 调度算法，访问磁道的顺序和移动的磁道数见下表。

<table><tr><td>被访问的下一个磁道号</td><td>移动距离(磁道数)</td></tr><tr><td>120</td><td>20</td></tr><tr><td>30</td><td>90</td></tr><tr><td>50</td><td>20</td></tr><tr><td>90</td><td>40</td></tr></table>

3)【标准答案】

采用FCFS（先来先服务）调度策略更高效。因为Flash半导体存储器的物理结构不需要考虑寻道时间和旋转延迟，可直接按I/O请求的先后顺序服务。
---

## 解题思路

索引结构计算各级容量，直接+一级+二级+三级地址项逐层扩展。访盘次数=查目录+读节点+读数据块。磁盘调度算法按寻道距离优化。


### 易错点1：间接地址块容量计算遗漏指针开销
❌ 错误：4KB块全存数据地址，容量4KB。
✅ 正确：每个地址4B，一级间接块存1024个地址，指向1024个数据块，容量1024×4KB。

### 易错点2：访盘次数计算
目录已在内存不计入；索引节点和数据块各计1次；间接寻址需读间接块。

## 易错点分析

