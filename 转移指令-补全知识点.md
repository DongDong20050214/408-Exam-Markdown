# 转移指令：408真题知识库 — 补全知识点 + 修复图谱连通性

> 给新对话的接手指令。新对话开场说一句「读 `转移指令-补全知识点.md` 并执行」即可。
> 仓库：`C:\Users\86172\Documents\Obsidian Vault\408真题知识库`（同时是 GitHub 仓库 DongDong20050214/408-Exam-Markdown，分支 main）

## 背景（已完成，勿重做）

Obsidian 图谱原本大量孤岛，根因是上游把 `[[wikilink]]` 批量转 Markdown 链接时转坏。前几轮已完成并**全部推送到 GitHub**：
- 第一阶段修复 434 文件：别名残留坏链清零、选择题→真题原卷年份链接、Index/README 枢纽（`e367542`）
- 112 道综合题新增「真题出处」年份链接（`6f3cd80`）
- 62 个高频概念的「关联知识」重链到现有库（`2b157c5`）
- **索引视图 856 条链接按题号重定位、显示文本换成真实题名**（`c24cd71` 起）

题目主体、综合题、索引页都已挂在主图上。**剩余的孤立点/小团体来自知识点这一层**，需要下面几步根治。

> ⚠️ **着重说明 —— 索引视图的「占位名断链」问题（已修，留作参考）**
> `04-索引视图` 下的「选择题-按考点/年份/学科索引」原本**对外几乎毫无链接**：链接目标用了 `2011年第01题-未分类`、`2010年第01题-线性表` 这类**占位/猜测名**，题库里根本没有同名文件 → 全部断链；还混有反斜杠路径，GitHub 不识别。这是该索引页在图谱里几乎孤立的直接原因。
> 已用 `fix_index_links.ps1`（从显示文本提取「年份+题号」，去题库按题号定位真实文件——题号唯一所以可靠）一次性修复 **856 条、未匹配 0**，并把显示文本也换成真实题名。
> **新对话注意**：凡是「链接文件名是占位/猜测、与题库实际不符」导致的断链（其他导航页、综合题索引同理），直接 `pwsh -File fix_index_links.ps1 -Apply` 重跑即可，不要手改。

## 待办

### 第 1 步：补建 124 篇知识点笔记（用 haiku subagent 省 token）
- 待建清单：仓库根 `_concepts_report.txt` 的「需新建: 124」段，按学科分（操作系统 28 / 计算机网络 34 / 计算机组成原理 43 / 数据结构 19；「并行」归到组成原理避免重复）。
- 开 haiku subagent（model: haiku）批量生成。**建议串行开单个或低并发**——本轮多个并发触发了 429 限流。每个 subagent 负责一个学科。
- 每个概念用 Write 创建 `AI-知识点原子库\<概念名>.md`，**文件名必须严格等于概念原名**（如 `中断系统.md`、`存储器.md`、`TCP_IP模型.md`），否则第 2 步重链会失配。
- 格式照搬现有 `AI-知识点原子库\页面置换算法.md`：YAML 头（name/type/subject/chapter）+ `## 定义` / `## 核心概念` / `## 算法流程/关键公式` / `## 易混淆点`(表格) / `## 关联知识点`(内链) / `## 历年真题`。
- 内容按 408 考纲（王道全书水平），每篇 25–45 行，计算公式（CPI/MIPS/磁盘存取/复杂度等）务必准确。
- 「关联知识点」内链**只指向确定存在的概念**（本学科清单 + 现有库），不要再造断链。

### 第 2 步：重链剩余关联知识
- 运行：`pwsh -NoProfile -File relink_assoc.ps1 -Apply`（仓库根，脚本已就绪，含同义映射规则）。
- 它会把题目里剩余约 205 条「关联知识」坏链（`](知识库/...)`）重链到 `AI-知识点原子库`。
- 验证：`grep -r "](知识库/" 408题库/` 应为 0；`_assoc_unresolved.log` 应接近空。

### 第 3 步：修复现有知识点笔记的互链断链
- 现有 100 篇知识点笔记的「关联知识点」互链，指向了若干**不存在**的笔记，例如：`线性表`、`贪心算法`、`数据压缩`、`段页式存储`、`信号量`、`顺序查找`、`折半插入排序`、`双端队列`、`线性表` 等。
- 处理策略：有 408 价值的概念顺手补建（同第 1 步格式），其余清理掉指向不存在笔记的内链。
- 目标：`AI-知识点原子库` 内部无断链，知识点之间连成网络（这是消除「小团体」的关键）。
- 可写个脚本扫 `AI-知识点原子库\*.md` 的 `](X.md)` 内链，X.md 不存在的列出来统一处理。

### 第 4 步：提交并推送
- `git add -A`
- `git commit -m "feat: 补全124篇知识点笔记 + 全量重链关联知识 + 修复知识点互链"`
- **push 必须先配 Clash 代理**（否则报 `SSL_ERROR_SYSCALL`）：
  ```
  git config http.proxy http://127.0.0.1:7897
  git config https.proxy http://127.0.0.1:7897
  git push origin main
  ```

## 现成工具（仓库根，已就绪）
- `analyze_concepts.ps1` — 重新统计概念映射/需新建清单（含同义映射表）
- `relink_assoc.ps1` — 关联知识重链（第 2 步用）
- `fix_index_links.ps1` — 索引视图链接按题号重定位 + 显示文本换真实题名（处理占位名断链）
- `add_synth_year_links.ps1` / `fix_links_v2.ps1` — 前几轮已用，无需再跑

## 验收标准
- `grep -r "](知识库/" 408题库/` = 0
- `AI-知识点原子库` 内部 `](不存在.md)` = 0
- Obsidian 图谱：知识点与题目连成网络，无大片孤立点
