# 对话执行指令

## 📋 当前状态

- ✅ 已完成：约375题修复（40.7%）
- ⏳ 剩余：约546题待修复
- 📦 GitHub仓库：https://github.com/DongDong20050214/408-Exam-Markdown
- 📂 工作目录：`C:\Users\86172\Documents\Obsidian Vault\408真题知识库`

---

## 🚀 立即执行

```
继续手工修复408题库剩余546题的错误。

已完成375题，剩余：
- 数据结构：约263题
- 计算机网络：约102题  
- 计算机组成原理：约114题
- 操作系统：约67题

执行要求：
- 启动3个并行Agent，逐题Read检查
- Agent超时就等待，严禁糊弄
- 上下文满时自动compact压缩
- 不要问问题，一口气执行完
- 每个Agent处理30题后自动停止，启动新Agent继续
- 每个agent全程手动read再查找问题最后修复问题
重点修复：
1. 题干不完整（补充选项、条件）
2. 图片路径错误导致无法显示图片
3. 图片缺失（标注或补充路径）
4. 元数据错误（剩余部分）
5. 重复文件（删除或标注）
6. 文档内有多处obsidian的wiki形式链接，可以转一下超链接更泛用一些

每修复100题提交一次到GitHub，持续执行直到100%完成。
```
## ✅ 完成标准

修复完成的标志：
- ✅ 0个"答案：需查看"
- ✅ 0个`.jpeg.md`路径
- ✅ 0个`[[Wiki链接]]`
- ✅ 0个不规范的question_id

---

## 💾 提交前检查

```bash
# 检查修改文件数
git status --short | wc -l

# 预览修改内容
git diff --stat

# 提交
git add -A
git commit -m "fix: 完成题库错误修复（第二批）

- 修复题干不完整：XX题
- 修复答案错误：XX题
- 修复元数据：XX题
- 删除重复文件：XX个"

git push origin main
```

---

## 🎯 最终目标

- ✅ 921道选择题100%无错误
- ✅ 113道综合题100%无错误
- ✅ 所有元数据规范统一
- ✅ 所有链接格式正确
- ✅ README和使用指南完整

---

## ⚠️ 关键注意事项

```

### 2. Agent的正确使用方式

**仅用于复杂判断**：
- 判断答案是否需要重新计算
- 分析题干是否完整
- 识别需要人工审核的题目

**不要用于批量修改**：
- ❌ 逐题修复格式错误 → 用批处理脚本
- ❌ 批量删除重复文件 → 用批处理脚本
- ❌ 统一替换内容 → 用批处理脚本/正则

### 3. Agent提示词模板

**如果必须使用Agent，用这个模板**：

```
检查题库中需要人工修复的题目，工作目录：`408题库/`

任务：
1. Grep搜索"答案：需查看"→列出文件路径
2. Grep搜索"题干缺失"→列出文件路径
3. 输出清单，不要逐个Read

仅输出需修复文件清单，不要执行修复操作。
```

**然后人工或脚本批量处理清单**。

---

## 🛠️ 推荐执行方案（手工修复）

### 执行流程

**第1轮（4个Agent并行，各30题）**：
```
Agent1: 数据结构 第1-30题
Agent2: 计算机网络 第1-30题
Agent3: 计算机组成原理 第1-30题
Agent4: 操作系统 第1-30题

完成后：git commit "fix: 第1轮修复120题"
```

**第2轮（4个Agent并行，各30题）**：
```
Agent1: 数据结构 第31-60题
Agent2: 计算机网络 第31-60题
Agent3: 计算机组成原理 第31-60题
Agent4: 操作系统 第31-60题

完成后：git commit "fix: 第2轮修复120题"
```

**持续执行**：
- 每轮120题
- 约需5-6轮完成546题
- 每轮提交一次

### 遇到问题处理

**Agent超时**：
- 等待120秒冷却
- 重启该Agent继续

**API速率限制**：
- 等待60秒
- 降低为2个Agent并行

**上下文满**：
- 不用担心，compact会自动压缩
- 继续执行即可

---

## 📊 进度检查命令

```bash
# 检查剩余错误
cd "C:\Users\86172\Documents\Obsidian Vault\408真题知识库"

# 检查题干不完整
grep -r "答案：需查看" 408题库/ | wc -l

# 检查图片路径错误（是否还有.md后缀）
grep -r "\.jpeg\.md\|\.png\.md" 408题库/ | wc -l

# 检查Wiki链接残留
grep -r "\[\[" 408题库/ | wc -l

# 检查元数据格式（缺少前导0）
grep -r 'question_id: "\d{4}-\d"' 408题库/ | wc -l
```

---

## ✅ 完成标准

修复完成的标志：
- ✅ 0个"答案：需查看"
- ✅ 0个`.jpeg.md`路径
- ✅ 0个`[[Wiki链接]]`
- ✅ 0个不规范的question_id

---

## 💾 提交前检查

```bash
# 检查修改文件数
git status --short | wc -l

# 预览修改内容
git diff --stat

# 提交
git add -A
git commit -m "fix: 完成题库错误修复（第二批）

- 修复题干不完整：XX题
- 修复答案错误：XX题
- 修复元数据：XX题
- 删除重复文件：XX个"

git push origin main
```

---

## 🎯 最终目标

- ✅ 921道选择题100%无错误
- ✅ 113道综合题100%无错误
- ✅ 所有元数据规范统一
- ✅ 所有链接格式正确
- ✅ README和使用指南完整

**预计剩余时间**：2-3小时（使用批处理脚本）

---

**上一个对话完成进度**：375/921题（40.7%）  
**本次对话目标**：完成剩余546题，达到100%
