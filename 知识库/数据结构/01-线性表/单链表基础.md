---
title: 单链表基础
knowledge_id: ds/list/linked/single
tags:
  - 数据结构/线性表/链表
  - 单链表

# ===== 以下为AI优化新增字段 =====
ai_summary: "单链表用指针连接各结点,插入删除O(1)但查找需O(n)。核心是指针操作,考试常考前插、后插、删除等指针修改"
difficulty_level: 3              # 中等难度,指针操作易错
exam_frequency: "高频"
typical_questions:
  - "[[408题库/02-按知识点-选择题/数据结构/01-线性表/2024年第01题-单链表]]"
  - "[[408题库/02-按知识点-选择题/数据结构/01-线性表/2023年第02题-单链表]]"
  - "[[408题库/02-按知识点-选择题/数据结构/01-线性表/2021年第01题-单循环链表]]"
---

# 单链表基础

## 定义

单链表是一种**链式存储**的线性表，每个结点包含：
- **数据域**：存储数据元素
- **指针域**：存储直接后继结点的地址

## 结点结构

```c
typedef struct LNode {
    ElemType data;      // 数据域
    struct LNode *next; // 指针域
} LNode, *LinkList;
```

## 基本操作

### 头插法
```c
// 在链表头部插入新结点
// 时间复杂度：O(1)
// 应用：逆序建表
```

### 尾插法
```c
// 在链表尾部插入新结点
// 需要尾指针，时间复杂度：O(1)
// 应用：正序建表
```

### 插入操作
```c
// 在第i个位置插入结点
// 时间复杂度：O(n)
```

### 删除操作
```c
// 删除第i个结点
// 时间复杂度：O(n)
```

### 查找操作
```c
// 按位查找：O(n)
// 按值查找：O(n)
```

## 带头结点 vs 不带头结点

**带头结点**：
- 优点：统一处理第一个结点，简化算法
- 头结点不存储数据

**不带头结点**：
- 第一个结点直接存储数据
- 需要特殊处理第一个结点

## 优缺点

**优点**：
- 不需要预分配空间
- 插入删除不需要移动元素
- 便于扩展

**缺点**：
- 不能随机存取
- 需要额外空间存储指针
- 查找效率低

## 相关真题

```dataview
LIST
FROM "408题库/02-按知识点-选择题"
WHERE contains(file.content, "#数据结构/线性表/单链表")
SORT file.name
```

## 知识点链接

- [[2023年第02题-双向链表]] - 扩展
- [[循环链表]] - 变形
- [[2023年第01题-顺序表]] - 对比

---

**待补充**：详细代码实现、常见算法题、图解

## 解题思路

### 第一步：理解题目核心
分析题目考查的核心知识点

### 第二步：逐项分析
逐一分析各选项，用✅❌标记正确与否

### 第三步：确认答案
综合分析得出答案

## 易错点分析

### 易错点1：常见误区
❌ 错误理解：
✅ 正确理解：

### 易错点2：概念对比
| 概念A | 概念B | 区别 |
|-------|-------|------|
| 说明 | 说明 | 区别点 |